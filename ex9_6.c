// Write a function digit(n, k) that return the kth digit (from the right) in n (a positive int).
// For example, digit(829, 1) returns 9, digit(829, 2) returns 2, and digit(829, 3) returns 8. If
// k is greater than the number of digits in n, have function return 0.

#include <stdio.h>

int main(void)
{
}
int digit(int n, int k)
{
	if (k > n)
	{
		return 0;
	}



