// Write a function named censor that modifies a string by replacing every occurence of 
// foo by xxx. For example, the string "food fool" would become "xxxd xxxl". Make the function as short as 
// possible without sacrificing clarity.
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(void)

char *censor(char str_arr[])
{	
	char *p = str_arr;
	while (*p) { 
		if (*p == 'f' && 'o' && 'o') {

		}
	}
}

